# объявление функции
def get_next_prime(num):
    while True:
        num=num+1
        s = [i for i in range(1,num+1) if num%i==0]
        if len(s) == 2:
            return num
            
# считываем данные
n = int(input())

# вызываем функцию
print(get_next_prime(n))
