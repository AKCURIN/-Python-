a = [i for i in input().split("-")]    #Вводим на тел номер сохраняем элементы разделителем служит чёрточка при этом сами - не сохраняются
d = 0
s = []    #Пустой список в котором будут размеры элементов списка а это нужно чтобы проверить соответствует номер стандартам может это не номер а ребёнок на клаватуре набрал
trafaret_1 = [1, 3, 3, 4]    #Первый стандарт тел номера
trafaret_2 = [3, 3, 4]    #2-й стандарт тел номера
b="".join(a)    #склеиваем наш список а во едино, т.е элементы склеиваем чтобы проверить...
if b.isdigit():    #...проверить является ли номер который мы ввели только численным, если нет то нет 
    for i in range(len(a)):    #пробегаем по списку а и считаем размер каждого элемента( элемент это то что разделено черточкой )
        s.append(len(a[i]))    
    if s == trafaret_1 and b[0] in '7':    #Сравниваем сходются ли размеры элементов тел номера стандартам и чтобы первая цифра была семёркой
                 print("YES")
    elif s == trafaret_2 :     #Проверяем возможно номер соответствует другому стандарту 
                 print("YES")
    else:
                 print("NO")
else:
    print("NO")    #Если содержаться буквы в номере то выходим на нет
