a = input().split('.')    #Вводим список разделяя элементы если будет точка
d=0    #Счётчик который нам нужен
for i in range(len(a)):    #В цикле прогоняем элементы и проверяем их на соответствие станартам ip 
    if 0 <= int(a[i]) <= 255:
        d+=1
if d == 4:        #Если всё хорошо и все 4 кода соответсвуют стандартам выведем да
    print("ДА")
else:
    print("НЕТ")
