a = [int(i) for i in input().split()]
if len(a)==1:
    print(1)
elif len(a)%2!=0:
    for i in range(1,len(a)-1,2):
        a[i-1],a[i]=a[i],a[i-1]
    print(*a)     
elif len(a)%2==0:    
    for i in range(1,len(a),2):
        a[i-1],a[i]=a[i],a[i-1]
    print(*a)    
