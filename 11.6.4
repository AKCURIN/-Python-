a = []
s = input()
s = int(s[1:])#Удаляем решётку которую вводят в первой строке для обозначения количества строк
for i in range(s):
    a.append(input())    #Вводим предложение и вносим его в список а
for j in range(s):    #Перебераем строки то есть елементы списка а
    if a[j].count("#") == 1:    #Проверяем если есть решётка в элементе списка т.е в строке 
        dolet = a[j].index("#")    #Находим расположение решётки 
        a[j]=a[j][:dolet]    #Делаем срез до решётки
        print(a[j].rstrip())    #Выводим строку без содержимого после решётки и с помощью rstrip() удаляем пробелы в конце нашей готовой строки
    else:
        print(a[j])    #Иначе просто печатаем исходную строку 
