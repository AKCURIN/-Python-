# объявление функции
def find_all(target, symbol):
    cou = [i for i in range(len(s)) if s[i]==char]
    return cou

# считываем данные
s = input()
char = input()

# вызываем функцию
print(find_all(s, char))
